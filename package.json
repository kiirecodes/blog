{
  "name": "0xkiire-coredumped",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build/build.js",
    "dev": "node build/build.js && echo 'Build done. Serve src/ with simple static server'",
    "start": "npx serve src -l 8080"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "marked": "^5.0.2"
  },
  "devDependencies": {}
}
